# Library navigation

Native macOS utility refinement: use the existing graphite/amber StudioTheme, system typography, small standard buttons, SF chevrons and 8/12px spacing. Add one persistent Previous/Next row under Library search; amber row tint identifies selection. No new panels, animation or custom chrome.

Single-click remains a preview; an exclusive double-click gesture activates the sound. Ultra entries recall the actual numbered device slot. Mac entries load the temporary edit buffer. Both capture the actual current hardware buffer to a recovery snapshot first. Navigation follows visible bank/search/folder/favorite results, stops at either end, disables during a switch and scrolls selection into view. No selection starts at the first result for Next or last for Previous. Existing data and stored presets remain intact.

Verify double-click delivery in the native app, next/previous across bank boundaries, filtered and empty lists, repeated actions while busy, transport failure, readback mismatch and disconnect during a scheduled program change.

Native review passed: both buttons fit the library sidebar, double-click activates the selected row, selection scrolls into view, and the existing editor remains readable. Generated public screenshots use synthetic offline presets.

The device cache adds a compact last-cached timestamp under the read/refresh row. Cached counts and the explicit Refresh label make saved data distinct from a fresh device read; offline browsing retains the same library layout.
